<UserControl x:Class="Qhta.SF.Tools.Views.SpecificValueEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Qhta.SF.Tools.Views"
             xmlns:resources="clr-namespace:Qhta.SF.Tools.Resources"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid x:Name="TextEdit"
  >
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
 <syncfusion:SfTextBoxExt 
        x:Name="ValueTextBox"
        Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type views:SpecificValueWindow}},
                      Path= TextValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        Margin="10,5"
        VerticalScrollBarVisibility="Auto"
        AcceptsReturn="True"
        Watermark="{x:Static resources:DataStrings.SearchedText}"/>
      <syncfusion:SfTextBoxExt 
        x:Name="ReplacementTextBox"
        Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type views:SpecificValueWindow}},
               Path= TextValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        Margin="10,5"
        VerticalScrollBarVisibility="Auto"
        AcceptsReturn="True"
        Watermark="{x:Static resources:DataStrings.ReplacementText}"/>
    </StackPanel>
    <StackPanel Grid.Row="2">
    </StackPanel>
  </Grid>
</UserControl>
