<UserControl x:Class="Qhta.SF.Tools.SpecificValueSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Qhta.SF.Tools"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <local:SelectableItemValueConverter x:Key="SelectableValueConverter" />
  </UserControl.Resources>
  <Grid>
    <ListBox 
      x:Name="ValueList"
      ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SpecificValueWindow}},
                                 Path= ItemsSource}"
      SelectedItem="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SpecificValueWindow}},
                                 Path= SelectedItem, Mode=TwoWay}"        
      Margin="10,5"
    >
      <ListBox.ItemTemplate>
        <DataTemplate >
          <TextBlock Text="{Binding Converter={StaticResource SelectableValueConverter}}" />
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
