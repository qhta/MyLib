<UserControl x:Class="MyLib.WPFControls.ComboBoxCheckItem"
             xmlns:my="clr-namespace:MyLib.WPFControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="comboBoxCheckItem"
             Height="auto" Width="auto">
  <StackPanel Orientation="Horizontal">
    <CheckBox 
      x:Name="internalCheckBox"
      IsChecked="{Binding IsChecked,ElementName=comboBoxCheckItem}"
      Tag="{Binding Tag, ElementName=comboBoxCheckItem}" 
      VerticalAlignment="Center" Margin="0,0,4,0"/>
    <TextBlock 
      x:Name="internalTextBlock"
      Text="{Binding Text, ElementName=comboBoxCheckItem}" 
      VerticalAlignment="Center"/>
  </StackPanel>
</UserControl>
